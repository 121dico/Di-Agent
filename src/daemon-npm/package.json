{
  "name": "di-agent-daemon",
  "version": "0.4.2",
  "description": "Di Agent daemon — connects to a Di Agent backend over HTTP/WebSocket and spawns local agent CLIs (Claude Code, Codex, OpenCode, OpenClaw).",
  "bin": {
    "di-agent-daemon": "./bin/di-agent-daemon.js"
  },
  "files": [
    "bin/di-agent-daemon.js",
    "cli/*.js",
    "README.md"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test test bin/*.test.js cli/__tests__/*.test.js"
  },
  "dependencies": {
    "ws": "^8.21.0"
  }
}
