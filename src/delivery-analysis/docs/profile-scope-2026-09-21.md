# Demo画像与当前页面的范围差异

用户截图前四张为召回任务184765378：dt=2026-09-16、entry_dt=2026-08-24、全部组，共2904人。后面Demo为2026-08-26的安心充低频购买人群214084人；HTML内标注源表charge_dw.app_charge_tag_duid_df、axc_freq_type=低频购买用户。两者不能直接比较人数或分类。Demo还含模拟分组及Mock效果，这些不能作为生产结果。

本次修正保留原图表结构：恢复维度专属说明、原标签顺序、是否会员标签、第四色灰色、自然语言示例；人数下明确任务/人群、进组日或快照日、组别。原“任务全量”侧栏改为任务ID与进组日。增加来源明细，展示画像真实来源表、字段、筛选、日期与去重口径。源标签原值、未知分类和缺失类别不伪造补齐；未验证的生产阈值不声称已验证。

后端portraitSource随实际selectTask选择返回：召回来源coupon_funnel、source_task_id+entry_dt；安心充画像来源audience_snapshot、crowd_id，独立于效果stat_dt。无额外上游请求，不修改已发布聚合值和去重方法。

验证：隔离当前HEAD与本次画像差异，50项测试通过；避免混入另一个进行中的问答组件改动。增加了单日范围、字段说明、来源弹窗及服务端两类来源契约回归测试。预发布通过正常主导航验证安心充频次说明、来源弹窗及召回任务切换。正式验证结果在部署后追加。

## 直接上游核查发现并修复跨组重复

上文2904是旧页面分组相加结果，不是整体去重人数。直接查询coupon源确认：2026-08-24 COUNT(duid)=2904、COUNT_DISTINCT(duid)=2900；对照组1460、实验组1444，各组内部唯一，但跨组重复4人。该13个进组日都存在不同程度跨组重复。现已用全部组独立去重重建单维与高维联合聚合，页面全部组总数、图表及交叉矩阵统一为2900。单组值保持原口径；页面明确显示组间非互斥。安心充独立控制查询194533人、重叠0。

修正后召回当日：生命周期老用户2894、成长期6；充电频次近30日未下单2900；身份私家车1752、营运车1148；会员未知1676、非会员1124、会员100。未借用Demo其他分类或人数。

构建时校验控制日期完整且不重复、分区匹配、各维度与整体去重守恒、联合桶逐维对账；缺失或冲突不发布。没有跨组去重的旧多组快照返回缺数，不回退到人数相加。不保留DUID明细。全部组画像查询只读预聚合。

Standards复核发现控制日期覆盖问题，已修复并回归；Spec复核无遗留。隔离基线53项通过，合并最新问答组件后59项全部通过，语法检查通过。正式页面通过完整透明代理进入正常主导航，核验2900、重复4人、会员分布与来源弹窗（coupon_funnel/member_status/entry_dt=2026-08-24）。

已增量部署四个文件及验证后的聚合快照，保留并发问答组件改动。备份：/root/delivery-profile-scope-backup-20260921.tar.gz。画像去重快照生成于2026-09-21T08:37:54.993Z（召回）、08:37:55.135Z（安心充）。本修复聚焦人群画像；分日效果的现有组内指标未重新定义。


## 侧栏溢出与 PRD 日期口径复核

2026-09-21 再次直查 coupon_funnel：唯一 dt=2026-09-16；entry_dt 包含2026-08-12至24共13天。最后一天独立去重2900，生命周期2894老用户+6成长期；activity_cycle分别1327（30-60天）、663（60-90天）、910（90-180天）。查询ID：ClickHouse_13f53ad3e9a34abab052ad20ef21e252（生命周期）、ClickHouse_662f8bd68fcb44b5957b49bbb4972c8f（频次）、ClickHouse_5c52e1dbf96047bd8ea087d90e346e8f（流失周期）。合同只提供字段中文名，未给生命周期生产阈值。

PRD 4.2要求所选目标人群画像，不要求仅最后一个进组日；5.3给出通用标签表charge_dw.app_charge_tag_duid_df、normal_cbill_order_cnt_30d按0/1-9/10-19/20-29/>=30分档；5.3.1活动8月12日至24日，按每日dt+DUID关联圈选条件与Apollo分流，Apollo UID还须映射DUID。历史验证明确要求保留投放前标签快照，但没有明确规定所有投放前画像统一T-1。当前实现是单进组日来源画像，不能宣称完成了投放前全人群画像。现有API缺少能核验历史画像的标签日期及订单原始字段，也未核验与三个Ditag包的成员映射；不能把9月快照冒充8月投放前快照。

侧栏根因：元信息隐式grid被长标题和不换行人数撑大，内容宽219px超过容器201px，被侧栏overflow:hidden裁切。改为minmax(0,1fr)轨道、完整标题换行、Ditag名称/人数可换行排布。来源弹窗补充生命周期与流失周期区别及人群包未筛选说明。
