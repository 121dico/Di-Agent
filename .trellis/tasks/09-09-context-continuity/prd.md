# 我的消息计量与连续上下文
用户已确认上一轮完整方案并要求实现、沿用线上同步授权。
- 底部计当前登录用户输入和各 Agent 公开回复的可见未撤回文本，标“聊天内容 · 文本分词/估算”，分别列输入和回复，不与模型容量相除。全历史统计，非仅已加载分页；附件正文不伪装计入。
- 详情保留实际窗口、原生用量、压缩/恢复事件。
- 复用原生自动压缩；持久化可见整理事件；保持历史和同一产品会话。
- 修复Codex真实thread恢复、模型按轮切换，不能因改模型静默删掉会话。无法恢复时明确失败保留会话，不自动清空或重放工具。
- 必须重建时使用已保存的交接材料恢复；稳定平台说明避免重复注入。保护用户明确约束和最近对话，不做字符中间截断。
- 用户新澄清：容量不强行扩大。初始化发现配置模型，实际请求确认模型/窗口；重点分别准确统计用户文本与Agent公开回复。匹配官方tokenizer才标重分词准确，其他情况明确估算。

测试边界承接用户已确认方案：权限内计数接口；CLI公开parse/spawn/dispatch接口；流事件归约回放；UI主计数与详情、真实请求/刷新/连续问答。

UI Brief（myuiskill细节模式，决策由既有确认推导，无开放问题）：沿用ContextUsageFooter位置、字体和详情抽屉；主数字“聊天内容”去掉模型百分比；估算/文本范围使用说明；真实模型详情和整理记录为次级信息；既有focus、CSS tokens、响应式布局、抽屉动效保持。错误=暂不可用，空=0条；不得把未知变成0。Gate0–5和7依据既有页面和确认范围auto-approved；Gate6、8、9待真实页面验证。无新增设计依赖。
