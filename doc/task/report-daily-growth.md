# V1.2 每日单次增长

用户要求：累计净增改为每天单次增长，避免异常增长永久抬高后续趋势。

## UI Brief / 范围

- confirmed：V1.2 改为当天价敏人数减前一天人数；移除累计指标和累计曲线。
- confirmed：原始数值、负号、缺失日保留；极端值只退出正常刻度与拟合，不修改快照。
- assumed：沿用现有浅色卡片、字体、颜色、图例、响应式布局、交互和权限。不更换 UI 库，不改数据库/API。
- 参考：现有每日柱图用于异常标记；现有 SmoothChart 用于平滑、tooltip 和估计虚线。
- 入口：报表 → 价敏用户报表 V1.2 → 增量分析。查看每日曲线，悬停异常日查看原数值，刷新再查看。
- 状态：空数据沿用 Empty；首日和日历缺口不计算；零值与负值不丢弃。
- 生产范围：ReportSnapshotGrowth、SmoothChart 日增量检测策略，以及已有组件回归测试。

## Gates

0–5、7 auto-approved：用户口径明确，复用现有布局与全部语义 tokens，无新的样式选项。
6：复用既有图表，无需独立原型。
8：每日差分相关 15 项测试通过，构建通过；双轴审查中的百分比阈值问题已修正。
9：已部署，保留前端备份 /root/deploy-backups/report-daily-growth-HeNK9D/previous。正常登录浏览器刷新 → 报表 → V1.2，已读到「每日增长趋势」「每日单次净增」，不再出现累计文案。进一步滚动、截图及悬停验收被用户切换浏览器中断，未完全通过。
全量 vitest 命令误收集 Electron/Playwright；src 内有两项未通过（旧首日零值预期、章节顺序预期），其余 333 项通过。不声明全量测试全绿。

## 验证

沿用已有组件 props / renderer seam，覆盖每日差分、负值、缺失、一次增长不累积至后续日、末日极端值不撑大坐标。
测试先红：ReportSnapshotGrowth 原累计文案和累计数值断言失败；实现后通过。
