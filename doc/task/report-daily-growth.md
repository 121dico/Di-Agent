# V1.2 每日单次增长

用户要求：累计净增改为每天单次增长，避免异常增长永久抬高后续趋势。

## UI Brief / 范围

- confirmed：V1.2 改为当天价敏人数减前一天人数；移除累计指标和累计曲线。
- confirmed：原始数值、负号、缺失日保留；极端值只退出正常刻度与拟合，不修改快照。
- assumed：沿用现有浅色卡片、字体、颜色、图例、响应式布局、交互和权限。不更换 UI 库，不改数据库/API。
- 参考：现有每日柱图用于异常标记；现有 SmoothChart 用于平滑、tooltip 和估计虚线。
- 入口：报表 → 价敏用户报表 V1.2 → 增量分析。查看每日曲线，悬停异常日查看原数值，刷新再查看。
- 状态：空数据沿用 Empty；首日和日历缺口不计算；零值与负值不丢弃。
- 生产范围：ReportSnapshotGrowth、SmoothChart 日增量检测策略，以及已有组件回归测试。

## Gates

0–5、7 auto-approved：用户口径明确，复用现有布局与全部语义 tokens，无新的样式选项。
6：复用既有图表，无需独立原型。
8：每日差分相关 15 项测试通过，构建通过；双轴审查中的百分比阈值问题已修正。
9：已部署，保留前端备份 /root/deploy-backups/report-daily-growth-HeNK9D/previous。正常登录浏览器刷新 → 报表 → V1.2，已读到「每日增长趋势」「每日单次净增」，不再出现累计文案。进一步滚动、截图及悬停验收被用户切换浏览器中断，未完全通过。
全量 vitest 命令误收集 Electron/Playwright；src 内有两项未通过（旧首日零值预期、章节顺序预期），其余 333 项通过。不声明全量测试全绿。

## 验证

沿用已有组件 props / renderer seam，覆盖每日差分、负值、缺失、一次增长不累积至后续日、末日极端值不撑大坐标。
测试先红：ReportSnapshotGrowth 原累计文案和累计数值断言失败；实现后通过。

## 后续：两张每日曲线平滑拟合

用户 confirmed：每日单次净增、每日增长率都要平滑拟合。默认沿用当前 UI；7日邻域加权局部线性回归，窗口首尾使用可用样本，排除离群观测，真实缺失隔离窗口。散点和 tooltip 保留真实值，拟合值限制在邻域范围内，不写数据库。明确图例：曲线为拟合，散点/悬停为真实值。

生产范围：reportLocalFit.ts、ReportsView 两个日增量系列。其它图不变。原 Brief 的 tokens、权限、响应式、恢复和空态沿用；Gate 0–9 本次范围 auto-approved。

验证：新拟合测试先因模块缺失而红，实现后线性保持、噪声平滑、离群排除、缺口断开通过。排除已知两项旧预期失败后，74 文件335项 src 测试通过，生产构建通过。双轴审查无逻辑阻塞，导入分组小项已修正。

线上备份：/root/deploy-backups/report-local-fit-tQf9Dn/previous。正常登录 Edge → 刷新 → 报表 → V1.2 → 增量分析，1500×768 截图确认两张拟合线独立于原始散点。左图悬停2026-08-19显示13.4万人，右图2026-08-18显示0.04%，原始读数链路通过。移动端未新增验收，未改变布局。

## 后续：从 7 月 29 日开始

用户确认首日可能不准，V1.2 统一排除2026-07-28。仅在展示加载边界过滤，保存快照/API不删改。缓存首次发布、目录接续加载和短范围起点统一限制到2026-07-29；因此29日的人群分布保留、日增量不可计算，30日起与前一天比较。不影响其他报表。

回归测试先红后绿：首次缓存不闪现28日、目录不查询28日、仅有28日为空、缺失失败和取消保留、29日不以28日作基准。74文件337项通过（仍排除前述2项旧失败测试），构建通过。双轴审查无阻塞。

线上备份 /root/deploy-backups/report-start-july29-oQltjm/previous。正常刷新 → 报表 → V1.2，图表范围2026-07-29到09-10，第一笔日增量2026-07-30为138624；截图确认全量及订单人群趋势从29日开始。沿用既有UI，无布局或样式变动。
