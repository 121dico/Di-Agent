# 曲线从首个已知值开始

## 用户要求与范围

- confirmed：未记录不等于零，不能给曲线添加虚构的零起点。
- 人数曲线保留首个真实人数；首日无法比较的日净增与增长率留空，从首个可计算值开始画。
- 累计净增仍是相对首个快照的差值，首日基线为零不是人数为零，图上明确说明。
- 保留真实零、负值、缺失日断点、各图独立切换和图例交互。
- 不改变上游 API、数据库、权限、统计人群和累计净增定义。

## UI Brief / 验收

myuiskill 沿用默认选择模式；本次为已明确行为的缺陷修正，不引入新视觉方案。沿用现有布局、色彩、字号、响应式和交互。入口为报表 → 增量分析 / dt 历史趋势。

Gate 0–7：auto-approved，修正 SnapshotGrowth 和旧版 presentation 的首日占位值，移除“首日基准为0”误导文案；无结构或样式变动。

回归先红后绿：原始数据 [100,110,105] 原先生成日增量 [0,10,-5]；应为 [不可计算,10,-5]，增长率同理。另测真实零变化、单日、缺失日、图形首点和累计净增保持。

Gate 8–9：auto-approved。44 项相关测试通过，生产构建通过。实际 Edge 页面经刷新 → 报表 → 增量分析验收：7 月 28 日不再绘制占位零，7 月 29 日首个增量为 181,262；可见曲线从约 18 万开始，无零起点连线。dt 人数分布仍从首个真实记录绘制。未改变持久化数据与查询。

三路审查完成：修复了单日旧缓存的概览 NaN 显示和无用参数构建错误。明确可用的首日正/负/零增量仍保留；未知首日留空。发布备份：`/root/deploy-backups/report-observed-start-mmi6mj/previous`。
