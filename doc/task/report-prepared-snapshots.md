# 城市与订单档位预计算

## 用户长期偏好

初始化、每日快照生成允许较慢；打开报表与筛选响应优先。已写入根 AGENTS.md 的 Persistent reporting preference。

## 已部署实现（2026-09-15）

- V1.2 按 dt、城市持久保存汇总，不保存 DUID 明细。
- 保存全部有订单、1–10 笔、超过10笔的价敏等级分布；城市分组之和须与独立 COUNT_DISTINCT 总人数一致。
- 每日完整结果一次 UPSERT 发布；失败不覆盖既有成功日。缺失字段、重复分页、非法计数拒绝发布。
- 启动补历史、每天10点刷新最新日、手动刷新31天；任务串行，刷新不被启动补历史吞掉。
- 查询从持久快照/进程内缓存读取；订单档位和等级切换用前端已返回的 order_groups，不调用上游。
- 每个快照保留查询执行ID、请求、SQL和图表绑定；管理员核验重跑仍需白名单验证。

## 验证证据

- 部署代码：3f804f2、3f50956、6255f6b；服务器 10.190.52.164:8080，备份 /root/deploy-backups/report-prepared-oDlDtY。
- 2026-09-15 16:02 北京时间发布 2026-09-14 快照，共361个城市分组，随后继续倒序补历史。
- 真实页面：北京、1笔订单、极高价敏 = 2,899 人；同等级总人数7,993，占比36.27%。与 PostgreSQL 保存的聚合结果一致。
- 切换订单笔数未出现 order-cohort 网络请求。analytics 服务端日志：热读11.18ms；冷读199.59ms和326.46ms。不将此等同于全部设备的端到端耗时承诺。
- 独立6255f6b工作树后端 service/repository 测试通过，生产前后端构建通过。新增前端预计算测试3项通过、订单组件预计算交互测试通过。
- 全套测试另有既存失败：handler traversal预期403实际404；presentation首日缺基线预期0实际NaN。本任务未修改这些语义。

## 当前边界

首次历史初始化在后台继续，尚未声明所有历史日都生成完毕；已完成最新日可立即使用。多城市汇总基于单日用户城市归属守恒校验，不跨日期累加用户。试点站独立验证/管理员明细查询不属于本次城市订单档位预计算。
