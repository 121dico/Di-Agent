# 人群分布前置与趋势紧凑布局

用户确认：第三模块价敏人群分布对比前置到第二模块；第四模块曲线缩至截图中累计净增/增长率卡片的量级。

myuiskill 默认模式：复用截图双列卡片布局与既有样式，桌面两列，每图绘图区 270（原 360），第三图保持半宽，1000px 以下单列。保留标题、独立模式按钮、显隐图例、日期、悬停、空/错误态，无数据逻辑更改。

顺序及导航：01指标概览 → 02价敏人群分布对比 → 03增量分析 → 04每日标签快照趋势。锚点 ID 保持兼容。管理员权限/业务口径/持久化不变。

Gate 0–7 auto-approved：范围是现有报表布局、编号、导航、绘图尺寸。Gate 8–9 验证：组件顺序、独立模式和图例测试，生产构建，实际页面查看两列尺寸与导航顺序。移动端使用已有布局规则回退单列。
