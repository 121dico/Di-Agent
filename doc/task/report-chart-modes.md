# V1.2 趋势 / 真实数值双模式

## 11:45 正常增量趋势优先（取代异常截断高柱）
截图复现：45日普通值数万、中间1400万、末日1100万，普通柱仅2px。根因是孤立跳回检测不判断末日，漏判末日撑大坐标，已识别异常仍用高柱。本轮仅增量图追加区间极端分层：至少7点，以幅度中位数6倍、中心+8×1.4826×MAD、1中最大值为门槛，候选不超过20%时分离，和原孤立检测合并。不是源数据错误判定，不改快照/概览/净增口径。正常柱线性尺度+正常平滑趋势；异常不画柱，仅图框上方5px中性空心标记，点击/聚焦/悬停查看完整原值。末日异常不外推；缺失不补线。

myuiskill 默认模式；用户已明确正常趋势为主，异常只留小标记（defaulted，取代上一轮断轴高柱）。沿用浅色卡、语义色、图表宽高与响应式，权限/导航/数据不改。测试边界沿用公开 IncrementBarChart；Gate0–7 auto-approved，8–9待实际页面验证。新45日组件复现测试已red（2px，期望>80px）→green。

## 本轮展示调整（2026-09-11，已部署，视觉验收待完成）
用户要求筛出离群再平滑正常点，柱图同样处理，取消负数绘图区。此次覆盖既有 SmoothChart / IncrementBarChart 公开组件测试边界，不改 API、快照、权限或统计。

UI Brief：沿用现有浅色图卡、字号、语义色及响应式宽度；不改区块排布。正常观测以平滑线连接；跨离群日的连接用虚线明确为估计，原始空心异常点与提示保留；真实缺失仍断开（confirmed）。柱图用非离群幅度确定从零开始的线性尺度，异常超界柱截断并加断轴符号（defaulted，优于额外异常面板）。负增长用向上幅度、红色与向下符号，提示保留负号（defaulted，不隐藏真实下降）。正常点 tooltip 仍为实测值；估计线不写回数据。趋势模式保留独立缩放和不固定首尾，不显示纵轴数字。

沿用现有图表为视觉、交互、动效参考，不引入依赖；空/单点/缺失、图例、键盘聚焦、减少动态效果不变。入口：报表 → V1.2 → 增量分析 / 每日趋势；先组件红绿，再完整测试与生产构建，最后实际页面查看、悬停和模式切换。仅前端部署，保留回滚备份。Gate 0–7 auto-approved（上轮拟合断线决策被本轮需求取代）；Gate 8–9 待验证。

验证：14c703d 主实现，c9af2ce 修复真实模式空数据 Infinity 轴域（独立审查发现并经红绿验证）。72 文件 / 315 测试通过，生产构建通过。测试包括跨离群虚线、原始负号、线性柱比例、空/单点、日历缺口不补线。需求审查无阻塞；标准审查唯一 P2 已修复。最后成功产品步骤为定位已登录 Edge 的 Di Agent 标签，刷新后浏览器返回用户正在交互的冲突，未争用；本轮真实页面的曲线/柱图/tooltip 视觉验收仍 not fully passed，不能用测试代替。部署备份：`/root/deploy-backups/report-smooth-R5057H/previous`（本轮前），`/root/deploy-backups/report-smooth-final-5HNn8k/previous`（空态修复前）。未重启后台或修改数据库。

## 范围
每日人群规模、全量每日价敏分布、有订单每日价敏分布，共用展示切换，默认「趋势对比」。不改接口与数据库，也不触发重新查询。

## 口径
- 用户纠正不希望首尾归一重合，趋势坐标改为 (值 − 本曲线所选区间均值) / 本曲线总体标准差；不固定起点、终点或极值，不随机错位。
- 趋势隐藏纵轴数字，曲线高低不是人数、不是百分比增长；不能用不同曲线高度比较增长率。真正同比例的曲线仍可重合，不伪造差异。
- 恒定序列放 0，单点不造曲线，缺失/失败日期断开不补零。
- 提示保留真实人数；相邻日变化率 = (当日 − 前日) / |前日| × 100%。首日、前日缺失或为零时说明不可计算。
- 真实模式保留已有统一人数坐标，图例显隐状态切换时不丢失。

## 验收
组件测试覆盖大/小体量独立缩放、真实提示、日变化率、常量、缺失、单点、三图切换及图例状态。追加边界回归验证真实→趋势→真实的原始坐标恢复，以及 -50% / -100% 下降。

2026-09-11 第一版（eec740a，min/max 缩放）线上：正常导航「报表 → 价敏用户报表 V1.2 → dt 历史趋势」，载入已保存的 45 天（07-28 至 09-10）；三图默认趋势，点击真实数值时三图恢复真实轴，返回趋势有效。实际日期点 08-29 提示保留各级真实人数及 +0.62%、+0.49% 等日变化率。桌面 1500×766 截图检查通过，窄屏自动换行已实现，未单独进行手机真机验收。此记录不作为后续 z-score / 离群修正版的视觉验收证据。

标准审查 0 阻塞问题，需求审查 0 问题；小于 0.005% 的真实日变化率会因两位小数显示为 ±0.00%，不影响人数或曲线。

仅部署前端，无后台重启、无数据库变更。可恢复前端备份：`/root/deploy-backups/report-chart-modes-H8Y0OR/previous`。

## 离群点（历史版本，已被顶部本轮展示调整取代）
统一覆盖报表折线及柱图的曲线叠层。相邻前后日期必须真实连续且点值有限，只有明显跳出后回归的孤立点才作为候选；阈值取相邻基准绝对值的 3%、局部 MAD×1.4826×3、邻点差×3、数值容差四者最大值。不从首尾或缺失日推断异常，不自动删除持续新水平。

候选点保留原始值、日期、真实日变化率，空心点单独标记，主线在当日断开，不插值/不强行平滑。趋势均值/标准差不计候选，超出图框的标记仅边缘定位并显示说明。真实模式（包括柱图）保留原始值确定坐标范围，不裁切真实量级。异常点或前日涉及异常时，变化率仍真实计算但附提示。

该规则只提示统计上的疑似离群，不能证明上游 API 或源表错误；需业务核查。未改变源数据或已保存统计。

## 最终修正版验证状态
c03c6a5：71 文件 / 306 项前端测试通过，TypeScript 与生产构建通过；三方审查收敛。发现并修复柱图异常点坐标被裁切的审查项。补充组件验收包含持续阶跃不误判、端点/缺失不误判、3 日尖峰标记、正常点缩放不受尖峰牵引。

已替换线上前端（未重启后台），前一版备份 `/root/deploy-backups/report-chart-shape-vBNekq/previous`。浏览器多次返回用户正在切换页面的冲突，未继续争用操作；最后成功产品步骤是第一版真实模式/趋势模式及 tooltip 验证。最终 z-score / 离群版的真实页面视觉验收尚未完成（not fully passed），不能用组件测试替代。无真实上游数据被改写。

## 2026-09-11 线上重叠修复
fd09eb7：增量分析与有订单人群分布共用 `trendBlock` 的显式 `grid-row: 4`，导致桌面布局重叠。两处改为 `fullWidthBlock`，按文档顺序各自占行，保留原有宽度、曲线和数据口径。布局回归先复现重复行失败，再验证修复通过；完整前端 72 文件 / 310 测试及生产构建通过，三方提交后审查无阻塞。

已部署线上前端，未重启后端、未修改数据库。备份 `/root/deploy-backups/report-layout-0YyGJs/previous`。真实浏览器刷新后经「报表 → V1.2 → 有订单人群」载入保存的 45 天；1500×766 下检查分布标题、双环图、比较表及下方每日趋势，区块顺序正常且不再重叠。新版趋势和离群说明可见。此次布局验收通过；未在真实数据中单独复验离群候选交互，不能将布局验收等同于所有曲线边界的视觉验收。
