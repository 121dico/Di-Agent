# 报表区域筛选精简

## 用户确认范围

- 左侧城市搜索、推荐城市，右侧地图，压缩高度；移除时间范围与当前区域面板。
- 移除订单分布、场站验证及复购区的解释段落、校验成功提示、刷新时间说明；保留数据、标题、单位、错误和重试。
- 不修改 SQL、统计口径、后端缓存或权限。

## UI Brief / myuiskill 默认模式

沿用现有浅色报表与省级地图。左二右三列、地图220px、8个现有常用城市（非数据热度推荐），760px以下上下排列；搜索完整城市目录。已选省份为左侧可移除按钮；城市和省份继续自动应用。按钮保留键盘焦点，140ms反馈并支持减少动画。既有数据缺失与失败仍明确呈现，不伪装为成功。

用户决定为 confirmed；列比例与八城为 defaulted。参考现有报表配色/控件与现有地图行为，不引入库。Gate0–5、7按代码审查与既有参考 auto-approved，6采用直接生产小改，不另建原型。8–9等待构建、回归、实际页面验证。

测试边界沿用城市选择组件公开回调和渲染结果；SQL/API不变。浏览器需验证推荐城市、搜索、地图省份、重置及注释清理。

## 验证与发布

- 构建通过；地图/筛选/订单分布20项测试通过，Overview按无注释要求更新后通过。
- 全量370项回归仅遗留既有 reportV12Presentation 首日0/NaN口径测试冲突；未修改真实首日数据。
- 已部署线上前端，保留备份 `/root/deploy-backups/report-region-compact-3GV51G`；未修改数据库或重启后端。
- 实际从侧栏报表入口进入，桌面截图确认左城市右完整地图、无时间控件和当前区域。浏览器被用户切换标签多次，搜索输入、最终注释版本及小屏交互尚未完成实际页面验收；Gate8/9为partial，不宣称全面验收。
