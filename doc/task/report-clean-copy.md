# 报表去除注释性文案

用户要求：去掉截图“已载入保存的 dt 快照，共 45 天”及报表同类注释性文字。

UI Brief：沿用 myuiskill 默认模式，保持布局、样式、权限与数据计算不变。移除成功加载横幅、技术说明段落、冗长计算解释、重复操作提示；保留标题、日期、单位、图例、精简的独立刻度/绝对值/估计值标识，以及错误与空状态。

范围：报表概览、人群对比、增量图、dt 趋势图和图形共用组件。管理员数据源配置和字段契约说明不在范围内。

Gate 0–7 auto-approved：现有视觉系统复用，不新增组件或依赖。Gate 8–9 以组件测试、构建和实际页面为验收依据。重点验证成功提示消失但错误仍可见、图例切换和悬停读数保留。
