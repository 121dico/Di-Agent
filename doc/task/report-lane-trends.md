# 分层价敏趋势

## 最新调整：整合大图

用户进一步确认不拆小图：现使用单一 SVG 绘图区，共享时间轴、竖向网格与悬停指示线；取消分层卡片标题与分隔线，保留人数降序、各自上下限、等高波动区间。绘图高度480，ResizeObserver匹配容器宽度以稳定字号。此条替代下文早期独立80px小图方案。

最新验收：6项相关测试、生产构建通过。实际 Edge 正常刷新→报表→dt历史趋势，确认两张整合图，悬停2026-07-31显示各类准确人数。备份 `/root/deploy-backups/report-integrated-trends-Yxol2j/previous`。

用户确认：两个人群分布图按人数由多到少垂直排列，各曲线分配相同高度区间，使用各自真实波动上下限，不让大人群压扁小人群。

沿用 myuiskill 默认模式：趋势对比改为分层趋势；排序按最近有效人数降序。每层固定 80px 绘图区，共用日期范围，显示各自上下限、最新人数；悬停/键盘对齐同日真实读数。真实数值模式保留共轴。缺失日断开、常数居中、真实零保留、隐藏曲线独立。上游 API、权限、缓存均不改。

等高各层需要足够垂直空间，替代原先三维量级挤在270px绘图区的叠线；两卡继续并排且窄屏单列。不恢复已删除的人群规模图及大段注释。

验收：排序、大小量级绘图区相同、真实值提示、缺失/常数/零/空状态测试；生产构建；正常报表入口的趋势模式与真实值切换。Gate 0–7 auto-approved，8–9 以实测结果记录。

Gate 8–9：6 项相关测试、生产构建通过。Edge 1500×800 正常刷新 → 报表 → dt 历史趋势实屏确认：仅两张分布图，等高分层、独立人数界限、规模降序。点击全量图 2026-08-01 展示低价敏 312,113,018 人及其他层同日精确值。真实数值分支未改动；移动端只检查规则，未真机测试。备份 `/root/deploy-backups/report-lane-trends-nvAPFX/previous`。
