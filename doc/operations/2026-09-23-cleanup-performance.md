# 2026-09-23 线上整理与加载性能修复

## 已确认的问题

- 投放 bootstrap 返回全部查询留痕，页面只显示前50条。安心充响应 2,795,827 字节，召回响应 12,029,651 字节（解压后）。
- 投放 Agent 每800ms读取上下文，旧实现每次序列化包含全量留痕的对象，父页面又重新解析。
- 电脑列表与候选 Agent 请求共用 machineLoading，候选先完成会错误结束电脑加载状态；并发进入页面还可能重复请求。
- 投放数据请求没有超时或切换取消，网络悬挂时会持续显示加载。

## 修复及验证

- 首屏仅发送50条留痕和 evidenceTotal，完整记录保留在 published.json 与导出流程；原快照未修改。
- Agent上下文按快照对象缓存，父页面发现内容未变时跳过解析。
- 电脑和候选请求使用独立加载状态、各自复用未完成请求；首次电脑加载不显示空列表。
- 投放请求15秒超时，切换取消旧请求；过期结果不能覆盖当前选择。
- 前一版哈希静态资源在本次发布中保留，避免已打开标签页加载旧代码块出现404。

同一网络、同一已发布快照，修复后各3次在线请求：

| 首屏 | 修复前解压大小 | 修复后解压大小 | 修复后实测请求耗时 |
|---|---:|---:|---|
| 安心充 | 2,795,827 B | 161,331 B | 88 / 50 / 47 ms |
| 召回 | 12,029,651 B | 152,214 B | 49 / 48 / 103 ms |

这是接口耗时，不是完整浏览器渲染耗时。Edge已验证从首页切到投放可显示193,260人快照，再切智能体可显示3台电脑、8个Agent。

投放测试93项通过，新增切换/超时用例后对应UI文件25项通过；前端定向测试7项、模型Hook测试2项通过。TypeScript检查及Vite构建通过。清理了原有测试文件未使用的React导入。

## 文件整理

- 71项旧构建、后端备份、部署暂存，共1,216,373,278字节，移动至 `/root/di-agent-archives/20260923T015922201863Z/`。
- 每项记录原路径、归档路径、文件数、内容校验和；保留可恢复能力，未删除业务数据。
- 本次发布备份 `/root/di-agent-archives/performance-20260923T020044Z/`。
- 使用 `scripts/ops/archive_legacy_deployments.py` 预览、归档或恢复。当前服务路径不变。
- 源码职责与发布约束见 `production-layout.md`。本地其他未提交业务改动不清理、不回滚。

## 验证边界

用户的偶发“长时间打不开”在本轮Edge操作中未稳定复现，不能据此保证所有间歇性网络或电脑连接问题已消失。服务器日志另外出现文件浏览20秒超时，但不属于投放首屏和电脑列表请求，此次未擅自改动daemon。后台快照构建约25分钟且定时每30分钟触发；其速度不等于页面加载速度，本次保留既有业务计算。
